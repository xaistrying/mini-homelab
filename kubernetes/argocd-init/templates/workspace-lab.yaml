{{- with .Values.apps }}
{{- if .workspaceLab.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: workspace-lab
  namespace: {{ $.Values.namespace | default "argocd" }}
  # finalizers:
  #   - resources-finalizer.workspaceLab.argoproj.io
{{- with .workspaceLab.spec }}
spec:
  project: {{ .project | default "default" }}
  source:
    repoURL: {{ .source.repoURL }}
    targetRevision: {{ $.Values.targetRevision | default "HEAD" }}
    path: {{ .source.path }}
  destination:
    server: {{ .destination.server }}
    namespace: {{ .destination.namespace }}
  syncPolicy:
    {{- toYaml .syncPolicy | nindent 4 }}
{{- end }}
{{- end }}
{{- end }}
